{
  "name": "foldable-talk",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "bs s",
    "predeploy": "rm -rf dist && bs export --web foldable.md && bs export --web meetup-olivier.md -o dist/ol && bs export --web meetup-yohan.md -o dist/yl",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "backslide": "^3.0.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
